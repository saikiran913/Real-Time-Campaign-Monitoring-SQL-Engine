# Known Limitations

- This is a SQLite simulation only.
- There is no live API connection.
- There is no actual streaming data pipeline.
- There is no dashboard in Phase 1.
- There is no machine learning in Phase 1.
- Raw tables do not enforce primary keys or foreign keys yet.
- Alert rules are definitions only and are not evaluated in Phase 1.
