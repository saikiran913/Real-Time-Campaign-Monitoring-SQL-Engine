# Database

Create the SQLite database file locally by running the SQL scripts in DB Browser for SQLite. A suggested local database name is `campaign_monitoring.db`.

Do not commit a large database file unless it is specifically needed for a later phase.
